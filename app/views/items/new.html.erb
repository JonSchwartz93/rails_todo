<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for [@list, @item] do |f| %>
			Content: <%= f.text_field :content, class: 'form-control' %><br>
			<br>
			<%= f.submit "Submit", class: 'btn btn-primary' %>
		<% end %>
	</div>
</div>