<!-- Side Navigation Bar -->
   <!-- App Name/Logo -->
  <div id="sidenav">
    <% if logged_in? %>        
      <h3><%= link_to "Priorities Portal", current_user %></h3>
      <h3><%= link_to "Log Out", logout_path, method: "delete" %></h3>
    <% else %>
      <h3><%= link_to "Welcome to Priorities Portal!", root_path %></h3>
    <% end %>
  </div>